[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
  NITRO_PRESET = "netlify"

# TanStack Start + Nitro's Netlify preset creates the server function during
# Netlify's build. Keep the Functions directory blank in the Netlify UI so the
# generated framework function is used automatically.